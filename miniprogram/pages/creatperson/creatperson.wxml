<view class='container1'>

  <image class='image' src="{{tempFilePathsA }}" mode="widthFix" />

</view>


<view class="line1"></view>

<!--账号-->
<view class="inputView">
  <image class="nameImage" src="../../assets/user.png"></image>
  <text class="loginLab">姓名：</text>
  <input class="inputText" placeholder="请输入您的名字" bindinput="phoneInput" />
</view>

<view class="line"></view>

<view class='tipview'>

<text class='tip'>提示：请先输入姓名，然后上传照片！</text>

</view>

<!-- 按钮-->
<view class="loginBtnView">
  <button class="loginBtn" type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}" disabled="{{disabled}}" bindtap="uploadimag">

    <text>上传照片</text>

  </button>
</view>
